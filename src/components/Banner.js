import React from 'react'
import { useState } from 'react';

function Banner() {
    const [movie, setMovie] = useStatetate([]);

    return (
        <header>
            {/* <<< Background Image */}
            {/* <<< Title */}
            {/* <<< div > 2 buttons */}
            {/* <<< description */}
        </header>
    )
}

export default Banner
